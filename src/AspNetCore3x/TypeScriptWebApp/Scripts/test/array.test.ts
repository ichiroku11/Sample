import { Assert, Test } from "../unittestlib";

export const arrayTest = new Test()
	.fact("filter_試す", () => {
		// Arrange
		// Act
		// Assert

		// todo:
		Assert.equal(1, 1);
	})
	.fact("map_試す", () => {
		// Arrange
		// Act
		// Assert

		// todo:
		Assert.equal(1, 1);
	});
